manifest {
    name            = 'nf-resume-test'
    description      = 'Late-failing DSL2 pipeline to validate control-plane FAIL->RESUME cache reuse'
    mainScript      = 'main.nf'
    nextflowVersion = '>=24.04.0'
}
